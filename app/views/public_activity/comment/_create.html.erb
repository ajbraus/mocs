added comment:
<% if activity.trackable %>
  <%= activity.trackable.content %>
  <% if false %><%= link_to activity.trackable.content, activity.trackable %><% end %>
<% else %>
  which has since been removed 
<% end %>