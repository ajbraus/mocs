<%= form_for @update do |f| %>
  <% if @update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@update.errors.count, "error") %> prohibited this update from being saved:</h2>

      <ul>
      <% @update.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :subject %>
    <%= f.text_field :subject, class:"span5" %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <%= hidden_field_tag "post_id", @post.id %>

  <div class="actions">
    <%= f.submit "Send Update to Joiners", class:"btn btn-primary" %>
  </div>
<% end %>
